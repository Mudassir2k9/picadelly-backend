name: Deploy Strapi to Azure Web App
on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 🧾 Step 1: Checkout repository
      - uses: actions/checkout@v4

      # 🟢 Step 2: Set up Node.js
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      # 🧰 Step 3: Install dependencies and build Strapi
      - run: |
          npm ci
          npm run build
          npm rebuild sharp --platform=linux --arch=x64 || true

      # 📦 Step 4: Package build for Azure
      - run: zip -r artifact.zip . -x "node_modules/.cache/*"

      # 🚀 Step 5: Deploy to Azure Web App
      - uses: azure/webapps-deploy@v3
        with:
          app-name: asp-piccadilly-prod-www   # 👈 your Azure app name
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: artifact.zip
